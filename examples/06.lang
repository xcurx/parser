let myFun: fn(int, string, []number) [][]string {};

fn myFunc(foo: number, bar: string) []string {
    foo += 10;
    bar = "in a function";

    return bar + foo;
}

fn returnTheFunc() fn(number, string) []float {} {
    if (foo == 0) {
        return 5+5;
    }

    return fn(n: number, s: string) []string {
        return 077;
    }
}